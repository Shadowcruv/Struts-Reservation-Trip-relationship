package com.example.secondproject.model;

import org.hibernate.Session;
import org.hibernate.Transaction;
import org.hibernate.cfg.Configuration;

public class MyStartupBean {
//    public void init() {
//        // Initialization code to run when the application starts
//        System.out.println("MyStartupBean is initializing...");
//        Session session=new Configuration().
//                configure("hibernate.cfg.xml").buildSessionFactory().openSession();
//
//
//        Transaction t=session.beginTransaction();
//        int i=(Integer)session.save(new Reservation("Miracle","Asaba"));
//        session.save(new Trips());
//
//        t.commit();
//        session.close();
//        // You can put your startup code here
//    }

}
